version: '3.9'

services:
  cloudflared-tunnel:
    image: cloudflare/cloudflared
    restart: unless-stopped
    command: tunnel --url ${ADDRESS}
    networks:
      - network1
networks:
  network1:
    name: ${NETWORK}
    external: true
